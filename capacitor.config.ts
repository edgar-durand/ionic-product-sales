import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'product-sales',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
